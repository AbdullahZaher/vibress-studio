packages:
  - 'packages/*'
  - 'playground'

# pnpm 10+/11 blocks dependency build scripts by default. These packages must
# run their postinstall scripts to install platform binaries (esbuild) or the
# build/E2E toolchain fails in CI.
onlyBuiltDependencies:
  - esbuild
